# Build artifacts
/seekey
src/*.o
build/
*.o
*.a

# Generated compile database (paths are machine-specific)
compile_commands.json

# Generated configs (only keep seekey.ini.example)
seekey.ini
config.ini

# Unity build outputs
test_runner

# Wiki lives in its own repo (Seekey.wiki.git); keep it out of the main
# project so a normal clone doesn't download the whole wiki.
wiki/
